# cartoon-hero

En enkel nettside for å holde oversikt over gjøremål du vil prioritere fremover.

## Kom i gang

Åpne `index.html` direkte i nettleseren, eller start en enkel lokal server:

```bash
python3 -m http.server 8000
```

Deretter gå til `http://localhost:8000`.
